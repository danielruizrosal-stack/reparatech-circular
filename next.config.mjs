/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'encrypted-tbn0.gstatic.com',
      'via.placeholder.com',
      'placehold.co'
    ]
  },
};

export default nextConfig;
